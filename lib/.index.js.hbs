/**
 * {{pkg.description}}
 * @module {{pkg.name}}
 * @version {{pkg.version}}
 */

"use strict";


var Bud = require('./bud'),
    create = require('./create');

var bud = function(config){
    return create(config);
};

bud.create = create;
bud.Bud = bud;

module.exports = bud;
